# World History Archive — 작업 가이드

정적 사이트(지구본 UI, `index.html` 하나에 전부 들어있음) + `data/*.json` 콘텐츠 + 병행하는 노션 워크스페이스로 구성된 프로젝트. 실제 배포: https://kimyeowool.github.io/world-history-archive/

## 세션 분업 구조 (2026-09-13~)

사용자가 카테고리별로 세션(채팅방)을 따로 운영하기로 함. 6개 세션은 사이드바 "세계사 아카이브" 그룹으로 묶여 있고, 각자 아래 파일 하나씩을 전담한다.

| 세션 | 담당 파일 |
|---|---|
| 역사 (General History) | `data/history.json` |
| 종교사 (Religious History) | `data/religion.json` |
| 철학사 (History of Philosophy) | `data/philosophy.json` |
| 예술사 (Art History) | `data/art.json` |
| 전쟁사 (Military History) | `data/war.json` |
| 정치사 (Political History) | `data/politics.json` |

`data/footnotes.json`은 6개 파일이 공유하는 용어 각주 사전 — 특정 세션 전담이 아니라 누구든 새 각주를 추가하면 여기도 같이 채운다.

**동시 편집 원칙**: 각 세션은 자기 담당 파일만 수정한다. 다른 카테고리 내용을 참조해야 하면(cross-category link) 해당 파일을 직접 Read/Grep해서 확인하고 링크만 건다 — 남의 파일 내용을 고치지 않는다. 같은 working directory를 공유하므로 git 저장소·노션 워크스페이스·이 CLAUDE.md·메모리 시스템은 6개 세션 전부에 자동으로 공유된다.

현재 각 파일이 어디까지 진행됐는지는 `.claude/PROGRESS.md` 참고 (스냅샷이라 시간 지나면 파일을 직접 열어서 최신 상태 확인).

## 로컬 미리보기

`.claude/launch.json`에 정의된 정적 서버(포트 8000). `preview_start({name:"world-history-archive"})`로 켜서 브라우저 도구로 확인. 오래 놀리면 서버가 죽어있을 수 있음 — `navOk:false`면 재시작.

## data/*.json 스키마

노드 하나는 대략 이런 모양:

```json
"노드 제목": {
  "desc": "본문. \\n으로 문단 구분. 하위 페이지를 언급하는 줄은 children의 키와 '정확히' 똑같은 문자열이어야 자동으로 링크가 걸린다.",
  "artwork": { "file": "assets/art/파일명.png", "alt": "...", "caption": "...", "desc": "..." },
  "links": { "본문에 실제로 등장하는 앵커 텍스트": ["카테고리", "...", "타겟 노드 제목"] },
  "footnotes": { "본문에 쓴 단어": "footnotes.json의 키와 동일해야 하는 값" },
  "children": { "하위 노드 제목": { ... } }
}
```

- **`artwork`**: 단일 객체 또는 배열(갤러리, ◀▶ 내비게이션 + "N/전체" 카운터로 렌더링).
- **`groups`**: `[{"header": "그룹명 또는 null", "items": ["자식 키", ...]}]` — children을 화면에 어떤 순서/묶음으로 보여줄지 결정. **`groups.items`의 문자열과 `children`의 키는 반드시 정확히 일치해야 함.**
- **`links`**: 카테고리 간 상호 참조(전쟁사↔종교사↔철학사↔예술사↔역사 등). path 배열은 최상위 카테고리명부터 시작해 타겟 노드 제목으로 끝난다.
- **`footnotes`**: `{본문 속 표기: 사전 키}` 형태이며, **그 "값"이 `data/footnotes.json`의 키로도 반드시 존재**해야 실제로 팝업이 뜬다. 이 이중 등록을 까먹는 게 반복되는 버그 원인이었음 — 각주 추가할 때마다 `footnotes.json`도 같이 편집할 것.
- **`inlineToggles`**(religion.json 기독교 노드 등에서 관찰됨), **`annotations`**(philosophy.json 아테네 학당 그림처럼 인물 라벨을 좌표로 붙이는 필드) 같은 특수 필드도 있음 — 정확한 동작은 실제 사용례를 직접 찾아보고 따라할 것.
- **⚠️ 노드 제목(키)을 바꿀 때**: `desc`의 미니 목차 줄, `groups.items`, 그리고 **노션 페이지 제목(속성)까지 셋 다** 같이 바꿔야 한다. 노션 `update_content`/`insert_content`는 본문 텍스트만 바꾸고 페이지 제목 속성은 안 건드리므로, JSON 키만 바꾸고 넘어가면 노션과 사이트 제목이 어긋난다 (2026-09-13에 십자군 전쟁 하위 페이지에서 실제로 발생했던 버그).

## 콘텐츠 작성 규칙 (전 카테고리 공통)

- 연도 표기는 "BC 335년" 형식. "기원전 335년" 쓰지 않는다.
- 용어 설명은 괄호 병기("단어(설명)") 대신 각주로 — 본문엔 단어만 자연스럽게 쓰고 footnotes로 링크.
- 인물 페이지는 별도 "생애" 하위 페이지를 만들지 않는다 — 메인 desc 자체가 전기이고, 저작·사건은 본문 안에 인라인으로 녹인다(소크라테스 패턴). 인물 서술의 기본 표준.
- 짧은 연표형 항목(역사 카테고리의 개별 사건 등)은 480~520자, 기승전결 + 다음 사건으로의 연결 문장으로 마무리. 깊이보다 폭 우선.
- 창시자가 명확한 종교(예수/무함마드/석가모니)는 탄생~죽음을 아우르는 500~800자 단일 페이지. 힌두교·유대교처럼 창시자가 불명확한 경우는 해당 없음.
- 세계사적 대사건(십자군 전쟁, 흑사병, IS, 9·11, 홀로코스트급)은 예외적으로 ~1000자까지 늘리고 대중문화 연결 + 실존 인물 위주로 서술.
- 분량 기준(비주류 인물/주제 ~3,000자 / 이름 있는 영역 ~6,000자 / 네임드 인물 ~8,000~10,000자)은 철학사처럼 인물 비중이 큰 카테고리 기준 — 다른 카테고리는 성격에 맞게 유연하게.
- 고유 병종(전쟁사) 같은 유닛 페이지는 하위 섹션마다 각자 artwork를 넣는다. 이미지 우선순위: 명화 > 일러스트 > 복원도/재현도.
- artwork를 넣었으면 노션 쪽 해당 페이지의 커버 이미지도 같은 그림으로 맞춰준다 — JSON에만 넣고 노션 커버를 빼먹는 실수가 잦았음.

## 노션 동기화

이 프로젝트는 사이트 JSON과 노션 워크스페이스를 항상 양쪽 다 최신으로 맞춘다(한쪽만 고치고 끝내지 않는다).

- `notion-update-page`의 `update_content`(검색-치환) / `insert_content`(맨 앞·뒤 삽입)로 본문을 고치고, `cover`로 표지 이미지를 같이 설정할 수 있음. `replace_content`(전체 교체)는 페이지에 이미지 블록이 있으면 서명된 URL을 안전하게 재구성 못 해서 위험 — 쓰지 말 것.
- 한글을 수동으로 \u 이스케이프해서 만든 긴 문자열은 글자가 소리 없이 깨지는 경우가 반복적으로 있었다(예: 궤멸→굌멸, 뱃삯→뱰샯). **old_str/new_str은 짧고 눈으로 바로 읽히는 평문 한글로 쪼개서 쓰고, 쓰고 난 뒤엔 반드시 다시 fetch해서 눈으로 오타를 확인**한다. old_str 쪽 오타는 "No matches found" 에러로 잡히지만 new_str 쪽 오타는 도구가 성공 응답을 줘도 실제로는 깨져 있을 수 있어 더 위험하다.
- 노션에 첨부된 이미지를 가져올 때 나오는 signed S3 URL은 5분 안에 만료된다 — 바로 다운로드할 것. 만료됐거나 서명이 안 맞으면(파일 크기가 이상하게 작으면 의심) 페이지를 다시 fetch해서 새 URL을 받는다. **다운로드 스크립트를 별도 파일로 Write하는 중간 단계를 거치면 그 사이에 5분이 지나버리기 쉽다** — fetch 결과가 온 직후, 바로 다음 tool call에서 curl로 받을 것(스크립트 파일 경유 없이 URL을 그대로 커맨드에 박아서 실행).

## 사용자가 채팅에 붙여넣은 사진 저장하기

이 세션(들)은 사용자의 실제 로컬 머신에서 돌아가므로, PowerShell `[System.Windows.Forms.Clipboard]::GetImage()`로 사용자가 자기 화면에서 복사하는 이미지를 그대로 받아 저장할 수 있다. 백그라운드로 폴링 스크립트를 돌려 MD5 해시로 중복 제거하면서 순서대로 저장 — 자세한 구현은 개인 메모리(`feedback_clipboard_image_capture.md`)에 있음. 노션에 사용자가 직접 이미지를 첨부해준 경우엔 그 페이지를 fetch해서 나오는 signed S3 URL을 바로 다운로드하는 쪽이 더 간단하고 안전하다.

## 기타

- 항상 git push까지 끝낸다 — 커밋만 하고 멈추지 않는다(단, 스켈레톤/구조만 잡는 단계는 사용자가 요청하기 전까진 push 보류).
- 커밋 전엔 `git diff`로 의도한 부분만 바뀌었는지 확인하고, 가능하면 브라우저에서 라이브로 한 번 확인한다.
